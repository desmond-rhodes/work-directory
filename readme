Hot switch between working directories.

Install Guide

	sudo cp work-directory /usr/local/bin/work-directory
	sudo chmod a+rx /usr/local/bin/work-directory
	echo "alias wd='source work-directory'" >> ~/.bashrc
